# Copy this file to ".env" and fill the values before running the app.
# Vite requires client-exposed env vars to start with VITE_.

# Base API URL used by the frontend to call your backend
VITE_API_BASE_URL=https://api.example.com

# Optional: feature flags / analytics keys
# VITE_SENTRY_DSN=
# VITE_GA_MEASUREMENT_ID=

# Anything secret should be stored in a real secret manager for production
